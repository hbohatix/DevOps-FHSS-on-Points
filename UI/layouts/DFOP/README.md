# DFOP UI layouts

This directory is reserved for Enfusion UI layouts used by the map-frequency display and future voting UI.

No production layout is committed in v0.1-dev because the UI contract will be implemented after the server-authoritative frequency state and replication layer are stable.

When Workbench creates `.meta` files for UI resources, commit them to the repository.
